export interface WeatherData {
  temp: number;
  description: string;
  sunrise: number;
  sunset: number;
}

export interface Settings {
  favoriteCity: string;
  textColor: string;
  backgroundColor: string;
}
